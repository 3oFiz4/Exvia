<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".app.ExviaApplication"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:icon="${exviaAppIcon}"
        android:roundIcon="${exviaAppIcon}"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".presentation.widget.ExpenseAppWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="xyz.x3ofiz4.exvia.ACTION_UPDATE_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/expense_app_widget_info" />
        </receiver>

        <activity
            android:name=".presentation.widget.WidgetEntryActivity"
            android:exported="true"
            android:theme="@style/WidgetEntryTheme"
            android:windowSoftInputMode="adjustResize|stateVisible" />

        <service
            android:name=".presentation.widget.ExpenseWidgetRemoteViewsService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />
    </application>
</manifest>
